
<%= render 'list', user: @user, book: @book %>

<h1>Books</h1>
<% @books.each do |book| %>

  <%= image_tag book.get_image %>
  <%= link_to book.title, book_path(book.id) %>
  <%= book.body %>

<% end %>
